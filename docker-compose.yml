version: "3.8"
services:
  app:
    build: .
    ports: ["5002:5002"]
    environment:
      - FLASK_PORT=5002
      - DATABASE_PATH=/data/predict.db
      - KELLY_FRACTION=0.25
    volumes:
      - predict-data:/data
    restart: unless-stopped
volumes:
  predict-data:
